name: Haskell CI

on: [push]

jobs:
  build:
   
    runs-on: ubuntu-latest

    steps:
    - name: Toolchain
      uses:  mstksg/setup-stack@master
      with:
        runs-on: ubuntu-latest
    - name: build
      run: stack build
    - name: Run tests
      run: stack test
