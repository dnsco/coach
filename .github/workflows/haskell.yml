name: Haskell CI

on: [push]

jobs:
  build:
   
    runs-on: ubuntu-latest

    steps:
    - name: Toolchain
      uses:  mstksg/setup-stack@master
      run: stack --version
    - name: build
      run: stack build
    - name: Run tests
      run: stack test
